{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch HimVeda (VS Code)",
            "type": "pwa-edge",
            "request": "launch",
            "url": "http://localhost:8080/",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start Application Server"
        }
    ]
}
